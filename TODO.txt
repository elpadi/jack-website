Each layout has a field for "issue".

On the issue page, fetch all layouts that belong to "issue" and allow for ordering.

Issue:
	string name:
	select visibility: (public | private)
	multi select layout_order: [name,name,...]

Layout:

	name:
	issue:
	rows:
	columns:
	orientation: (landscape | portrait) 
	images: ["number-face","number-face",...]
	
Images saved under directory images/full/{issue slug} with name sheet_number-sheet_face.jpg
Thumbnails saved under directory images/thumbs/{issue slug} with name sheet_number-sheet_face.jpg


