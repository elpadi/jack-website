{% macro object(name, text, class='') %}
<object class="svg-image svg--{{ name }} {{ class == '' ? '' : 'svg-' ~ class }}" type="image/svg+xml" data="{{ ('svg/' ~ name ~ '.svg') | asset_url }}">{{ text|default('') }}</object>
{% endmacro %}
