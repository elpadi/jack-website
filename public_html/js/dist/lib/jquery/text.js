define(["jquery"],function(jquery){function addToList(list,items){if(typeof items==="string"){list.push(items)}else{list=list.concat(items)}}$.fn.toggleText=function(texts){var state=this.data("toggleText");if(!state){var list=[this.text()];var attrValue=this.data(texts);addToList(list,attrValue?attrValue:texts);state=new ModularList(list)}else{this.text(state.next())}return this.data("toggleText",state)};return{}});